# Swift build artifacts
.build/
.build-linux/
*.o
*.d

# Xcode user state (never commit user-specific IDE files)
xcuserdata/
*.xcuserstate
.swiftpm/xcode/

# Cursor project settings (local-only)
.cursor/
.cursorrules

# Local git hooks (not shared via repo)
.githooks/

# Old nested package directory (contents hoisted to repo root).
# Leading slash anchors to repo root so Sources/VideoVortexCore/ is NOT ignored.
/VideoVortexCore/

# macOS
.DS_Store

# Local gather Step 4 smoke output (RapidTesting / DockerTesting pinned folders)
/gather-step4-smoke/
/gather-step4-smoke-linux/
/gather-step4-smoke-linux-zoo/

# Private docs
Docs/
.aider*
